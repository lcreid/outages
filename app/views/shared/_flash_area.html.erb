<% if @configuration_item.errors.any? %>
  <div id="error-explanation">
    <h2><%= pluralize(@configuration_item.errors.count, "error") %> prohibited this article from being saved:</h2>

    <ul>
    <% @configuration_item.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
